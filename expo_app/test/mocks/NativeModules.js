// Minimal NativeModules mock compatible with react-native and jest-expo setup
const NativeModules = {
  UIManager: {},
  Linking: {},
  NativeUnimoduleProxy: {
    viewManagersMetadata: {},
  },
};

module.exports = NativeModules;


